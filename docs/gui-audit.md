# GUI 점검과 고도화 (2026-08-26)

`src/gui/app.py` 760줄 통독 + 실행 화면 캡처로 점검했다. 결함 수리 · 엔진 설정 노출 ·
디자인 정비 세 갈래.

## 체크리스트

- [x] 1. 결함 수리 (아래 표의 B1~B17)
- [x] 2. 엔진이 지원하는데 GUI 에 없던 설정 노출 (촬영고도·전환속도·이륙보안고도·단순화·시각기록·이륙기준점·KMZ포장)
- [x] 3. 디자인 정비 (팔레트·타이포·카드·버튼 라벨·중첩도 2×2·상태 표시줄)
- [x] 4. 순수 로직 분리 + 테스트 (`build_overrides`·프리셋 직렬화 — Tk 없이 검증)
- [x] 5. 화면 캡처 전/후 비교
- [x] 6. 문서 갱신 · 커밋

## 발견한 결함

| # | 내용 | 근거 |
|---|---|---|
| B1 | **KMZ 미리보기가 절대 안 그려진다.** `if c_elem and c_elem.text` — ElementTree 요소는 자식이 없으면 거짓이고 `<coordinates>` 가 정확히 그 경우 | `bool(요소)=False` 실증 |
| B2 | **상태 표시가 아무 데도 없다.** `var_status` 를 여기저기서 set 하지만 어떤 위젯에도 묶여 있지 않다 — "실행 중/완료/지형팔로우 지원" 전부 허공에 | 전수 검색 |
| B3 | 언어 토글이 UI 를 통째로 재생성하면서 debounce 타이머 미해제(파괴된 지도에 콜백), 로그 소실, 실행 중이면 RUN 버튼이 활성으로 되살아남, 파일명 후보 소실, 지도 스타일 초기화 | 코드 추적 |
| B4 | 속도 입력에 `to_int` — **12.5 를 조용히 버리고 미주입** | 코드 |
| B5 | 필드 스캔 실패 시 가짜 후보 `["ADDRE_1_2","name"]` 를 주입 — 존재하지 않는 필드를 고르게 함 | 코드 |
| B6 | 프리셋 저장/로드가 **2개 필드만** 다룬다 (고도·기종). 나머지는 주석 "..." | 코드 |
| B7 | 배치 오류의 traceback 이 stderr 로 가서 GUI 로그에 안 보인다 (stdout 만 리다이렉트) | 코드 |
| B8 | 미리보기가 파일 내 전체 피처를 **병합 후 최대 폴리곤 1개**만 그림 — 배치는 피처당 KMZ 하나를 만드는데 미리보기는 거짓말을 한다 | 캡처: 4필지→1덩어리 |
| B9 | `poly.add_position(...)` — "force redraw? no." 주석이 달린 무의미 코드(중복 꼭짓점 추가) | 코드 |
| B10 | 입력 폴더가 무효면 이전 폴리곤을 지우지 않고 반환 — 김 빠진 화면 유지 | 코드 |
| B11 | 시작 시 미리보기 미실행 — 변수 "변경"만 트리거라 기본 경로에 파일이 있어도 빈 지도 | 코드 |
| B12 | 배치 실행 중 창 닫기 → 데몬 스레드 즉사, 쓰다 만 KMZ 가능 | 코드 |
| B13 | 실행 전 사전 점검 없음: 입력 폴더 부재도 스레드 안에서야 실패. danger 판정이어도 그냥 실행 | 코드 |
| B14 | 폴더 선택 대화상자 시작 위치가 `src/gui/` | 코드 |
| B15 | 변수만 있고 위젯이 없는 설정 6종(전환속도·이륙보안고도·단순화·시각기록·이륙기준점·KMZ포장). `pack_kmz` 는 배치에 전달조차 안 됨 | 전수 검색 |
| B16 | 버퍼 칸을 비우면 `geometry_buffer_m=None` → 엔진에서 `buffer(None)` 크래시 | 코드 추적 |
| B17 | 지형팔로우 판정이 부분 문자열 매칭 — 값이 드롭다운 출신이므로 정확 매칭이 맞다 | 코드 |

## 하네스가 만든 가짜 결함 (기록)

첫 캡처에서 지도가 하얗게 나와 "기본 타일 서버가 죽었다"로 갈 뻔했다. 타일 서버 4종은
전부 정상(200, 0.15~0.55s)이었고, 진범은 **캡처 하네스가 `mainloop()` 대신 `update()`
펌프로 돌던 것** — 그러면 tkintermapview 의 타일 스레드가 `ImageTk.PhotoImage` 를 만들
때마다 `RuntimeError('main thread is not in main loop')` 로 죽어 빈 타일만 남는다(12/12
실증). 하네스를 mainloop + after 로 고치자 지도는 멀쩡했다.

교훈 그대로: **검증 장치가 결함을 만들 수 있다.** 같은 이유로, 앱 본체에서도 메인 루프를
오래 막는 작업(동기 GPKG 파싱 등)이 길어지면 그 사이 타일이 빈 채로 남을 수 있다 —
미리보기 파일 수를 20개로 제한하는 이유이기도 하다.

## 고도화에서 뺀 것 (기각·보류 기록)

- **미리보기의 스레드 분리** — 보류. tkintermapview 가 스레드 안전하지 않아 파싱만
  분리해도 그리기는 메인 루프로 돌아와야 한다. 파일 20개 · 피처 150개 제한으로 체감
  문제가 없어 복잡도를 지불하지 않았다.
- **GPKG 레이어 선택 UI** — 보류. 엔진은 `layer` 인자를 받지만 실무 입력(필지 GPKG)은
  단일 레이어라 수요가 확인되면 넣는다.
- **진행률 %** — 기각. 엔진이 전체 개수를 먼저 세지 않아 분모가 없다. 대신 완료 시
  상태 표시줄 갱신으로 충분.
- **기본 비행 속도 10→15 변경** — 기각. 포트폴리오 카드가 "기본 10"으로 이 도구를
  소개하고 있어 어긋나게 된다. 사용자가 15로 쓰는 것은 프리셋이 담당할 몫.

## 결과 (2026-08-26)

- 결함 B1~B17 전부 수리. `app.py` 760줄 → 1,002줄 — 늘어난 만큼은 노출된 설정 7종의
  위젯, 번역 키, 그리고 순수 로직 분리분이다.
- 테스트 33 → **48개 전부 통과** (GUI 순수 로직 11 + 재생성 스모크 4).
  스모크 4종을 돌연변이로 확인: 로그 보존 제거·상태 라벨 분리·TF 게이트 무력화·
  빈 버퍼 강제 제거 → 각각 정확히 1건씩 잡힌다.
- GUI 실행 경로를 하네스로 실주행: RUN → 스레드 → 로그창 스트리밍 → 상태 "완료" →
  피처당 KMZ 4개 + HTML 리포트. 명명 필드가 파일에 없으면 파일명 자동 폴백까지 확인.
- 화면 전/후 캡처 비교로 육안 확인 (필지별 색 구분 외곽선, fit 줌, 상태 표시줄,
  하단 고정 실행 버튼).

### 캡처가 잡아낸 2차 결함 (코드만 봐서는 몰랐던 것)

- **입력칸이 40px로 짜부라졌다** — 카드 하단 스페이서 `CTkFrame` 의 기본
  `width=200` 이 그리드 col0 최소폭이 됐다. 스페이서는 width=1 로.
- **실행 버튼이 첫 화면 밖** — 상세 설정이 길어지자 스크롤 아래로 밀렸다.
  실행/프리셋 버튼을 스크롤 영역 밖 하단 고정 바로 옮겼다. 주 동작은 항상 보여야 한다.

## 검증 방법

- 순수 로직(`build_overrides`·프리셋 직렬화·파일명 sentinel)은 Tk 없이 pytest 로.
- 재생성(언어 토글) 경로는 지도 위젯을 뺀 스모크 테스트로 2회 왕복.
- 화면은 mainloop 하네스로 전/후 캡처해 육안 대조.
- 기존 33개 테스트(변환 경로 포함)가 그대로 초록인지 확인.

## v2 리디자인 (2026-08-27)

"설정 나열"을 작업 흐름으로 재배치했다. 사용자 요청("자유롭게 설계")에 따른 자유 설계.

- **전역 헤더 바** — 브랜드·태그라인(GPKG → DJI KMZ · WPML 1.0.6)·작업 상태·EN/KR.
- **번호 매긴 워크플로** — ① 데이터(포맷은 세그먼트 버튼) → ② 미션 → ③ 상세 설정.
  상세는 **접이식이고 기본 접힘** — 매일 만지는 값이 아니고, 첫 화면에서 RUN 이 보여야 한다.
  접힘 상태는 재생성(언어 토글)을 건너 살아남는다.
- **안전 판정을 실행 존으로** — 판정 칩·GSD/블러를 RUN 버튼 바로 위, 결정하는 자리에.
- **스캔 요약 오버레이** — 지도 좌하단에 "파일 N · 폴리곤 M (· 생략 K)". 미리보기가
  이미 세던 값인데 버려지고 있었다. 실행 전에 무슨 일이 벌어질지 말해 준다.
- **리포트 열기 버튼** — 로그 헤더에서 가장 최근 report_*.html 을 바로 연다(os.startfile).
  리포트가 없으면 비활성.

### 기본 지도를 Esri 로 교체 (캡처가 잡은 환경 변화)

리디자인 캡처에서 CartoDB 타일 전면에 "API KEY REQUIRED" 워터마크가 떴다 — 전날
캡처까지는 정상이었으니 2026-08 시점에 익명 접근이 차단된 것. 죽은 기본값은 없느니만
못하므로 CartoDB 를 목록에서 빼고 **Esri Dark Gray(기본)** 와 **Esri Satellite** 를
넣었다(키 불요, 실측 정상). 옛 저장 스타일이 남아 있어도 Esri Dark 로 폴백한다.

참고: 처음 Esri 타일 검증에서 "전부 빈 타일"로 읽을 뻔했다 — 내 타일 좌표 계산이
8타일 북쪽 바다를 찍고 있었다. 좌표를 고치니 실그림이었다. 판정자(검증 스크립트)
자체를 의심할 것.

검증: 59개 테스트(접힘 상태 왕복·리포트 버튼 활성화 규칙 포함), GUI 실주행
(RUN → KMZ 4개 → 상태 "완료"), 기본/펼침 두 화면 캡처 육안 확인.

## 실물 데이터 검증 (2026-08-27)

제주 경매부지 실물 GPKG(83필지, 10m 버퍼 적용본)로 전 구간을 돌렸다. 데이터 자체는
`sample/` 에 있고 **추적하지 않는다** — 이 저장소는 public 이고 PNU·주소·사건번호가
들어 있다.

### 통과한 것

- 83 피처 → **KMZ 83개, 0.4초**, 실패 0.
- 좌표 전부 제주도 안(126.17~126.92E / 33.20~33.55N). EPSG:5186 → WGS84 재투영 정확.
- **면적 오차의 출처를 simplify 로 확정** (한 번에 한 변수만 바꿔 측정):

  | simplify | 면적 오차 평균 | 최대 | 꼭짓점 평균 |
  |---:|---:|---:|---:|
  | 0.0 m | 0.017% | 1.43% | 53.9 |
  | 0.5 m | 0.668% | 2.72% | 31.6 |
  | 2.0 m | 2.840% | 7.73% | 15.5 |

  simplify 0 에서 0.017% — 재투영 왕복은 사실상 무손실이다. 오차는 전부 단순화 몫.
- KMZ 내부: 루트에 template.kml·waylines.wpml, 고도 120m 가 KML 과 WPML
  executeHeight 26개 양쪽에, 속도·전환속도·이륙보안고도 양쪽 일치.
- **inspector 의 지리 경고가 실물에서 발동**: 임무 폴리곤(제주 동부)과 원 템플릿
  웨이포인트(제주 서부) 사이 **39.4 km**. 어제 만든 경고가 실제 데이터에서 작동한다.
- GUI 미리보기가 83필지를 개별로 그리고 스캔 요약이 "파일 1 · 폴리곤 83" 을 보고한다.

### 실물이 알려 준 것 (이론적 우려의 정정)

- **멀티파트 손실은 이 데이터에 없다.** 83개가 전부 `MultiPolygon` 타입이라 위험해
  보였지만 **조각은 모두 1개** — QGIS 버퍼 연산이 타입만 멀티로 남긴 것이다.
  "가장 큰 조각만 쓴다"는 제약은 이론적으로 유효하나 여기서는 손실 0.
- **명명 필드는 고를 때 조심해야 한다.** 83개 기준 고유성:
  `ADDRE_1_2`(기본값)·`PNU`·`ADDRE_1`·`field_2`·`area` 는 83/83 고유,
  사건번호 필드는 72개(한 사건에 여러 필지), **`SGBH`·`OX` 는 전부 NULL 이라
  고르면 82개가 덮어써진다.** GUI 드롭다운이 이것들을 후보로 내놓는다.
- **구멍(interior ring) 있는 필지 1건.** 34,349m² 중 485m²(1.4%). 현재 코드는
  `exterior` 만 쓰므로 구멍이 메워진다 — 그 위도 촬영 대상이 된다.
- 컬럼명 `사건번호` 가 원본에서 이미 깨져 있다(`사건번` + U+FFFD). UTF-8 12바이트가
  10바이트에서 잘린 것 — **DBF 컬럼명 10바이트 제한의 흔적**이다. 이 데이터는
  Shapefile 을 거쳐 왔다. 우리 도구는 깨진 이름 그대로 읽어 정상 동작한다.

### 남은 판단거리 (고치지 않음 — 실무 판단 필요)

- 명명 필드값이 겹치면 **조용히 덮어쓴다**. 로그는 "완료" 를 찍고 파일만 사라진다.
  이 데이터의 기본 필드는 고유하므로 당장 문제는 없다.
- 구멍을 임무에서 뺄지(도넛 폴리곤 지원) — DJI 템플릿이 innerBoundaryIs 를
  받는지 먼저 확인해야 한다.
